node_modules/
npm-debug.log*

# Static build output — regenerated by `npm run build` on every deploy.
dist/

# Platform CLI working directories
.vercel/
.netlify/

# Local-only secrets (.env and .env.production are committed build config)
.env.local
.env.*.local

# dist-wordpress/ is deliberately committed: the deployment team takes the
# packaged theme straight from the repository. Rebuild it with
# `npm run package:wp` after changing anything under wordpress/.
